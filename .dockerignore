# Keep the build context lean — only Go sources and the static web assets.
.git
.gitignore
.jj
.claude
.playwright-mcp
# Test/debug screenshots are excluded one layer earlier by .gcloudignore
# (anchored /test*.png /snake*.png). At Docker level we only need to skip
# the local build binary; sprite atlases under html/img/ must ship.
/flow/flow
*.md
*_test.go
Dockerfile
.dockerignore

# Mac noise
.DS_Store

# Editor / IDE
*.swp
*.swo
.idea
.vscode

# Source-only artifacts we don't ship
sprites/
.travis.yml
