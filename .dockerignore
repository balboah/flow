# Keep the build context lean — only Go sources and the static web assets.
.git
.gitignore
.jj
.claude
.playwright-mcp
# Test/debug screenshots live at repo root; the *.png sprite atlases under
# html/img/ must ship in the image, so don't blanket-exclude PNGs.
/*.png
*.md
*_test.go
Dockerfile
.dockerignore

# Mac noise
.DS_Store

# Editor / IDE
*.swp
*.swo
.idea
.vscode

# Source-only artifacts we don't ship
sprites/
.travis.yml
